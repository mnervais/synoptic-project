{
  "name": "interactive-community-events-map",
  "version": "1.1.4",
  "private": true,
  "author": "Michael Nervais",
  "scripts": {
    "start": "cd server && npm install && NODE_ENV=production node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
