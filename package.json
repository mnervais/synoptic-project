{
  "name": "interactive-community-events-map",
  "version": "1.1.0",
  "private": true,
  "author": "Michael Nervais",
  "scripts": {
    "start": "cd server && npm install && node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
